###############################################
# Auth settings
ARTHUR_CLIENT_ID: data-plane-d775e380-a904-4604-ac44-8778977501f8
ARTHUR_CLIENT_SECRET: replace_me
ARTHUR_API_HOST: https://platform.arthur.ai
###############################################
# General settings
FETCH_RAW_DATA_ENABLED: true
###############################################
# Engine Internal Shield Connection
GENAI_ENGINE_INTERNAL_API_KEY: ""
# host for connecting within the data plane
GENAI_ENGINE_INTERNAL_HOST: ""
# host for connecting to shield from users, may be different for envs like docker compose
GENAI_ENGINE_INTERNAL_INGRESS_HOST: ""
###############################################
# GenAI Engine max page size for data fetching
GENAI_ENGINE_MAX_PAGE_SIZE: 1500
###############################################
# Aggregation Config settings
SEGMENTATION_COL_COUNT_LIMIT: 3
SEGMENTATION_COL_UNIQUE_VALUE_LIMIT: 100
# Timeout increased to 10 minutes to allow for larger datasets and more complex aggregations
ML_ENGINE_AGGREGATION_TIMEOUT: 600
###############################################
# SSL verification for connections to ARTHUR_API_HOST. Defaults to true (see the
# Config.get_bool default at the call sites). The legacy KEYCLOAK_SSL_VERIFY env
# var is honored as a fallback for backward compatibility. Left unset here so an
# explicit KEYCLOAK_SSL_VERIFY override is not shadowed by a value from this file.
# ARTHUR_API_HOST_SSL_VERIFY: true
